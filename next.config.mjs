/** @type {import('next').NextConfig} */
const nextConfig = {
  // in order to use next image for better seo, for compression and resizing etc
  /*
  images: {
    remotePatterns: [
        {
            hostname: "ahsbdjhasbhjdbajshbdjha.public.blob.vercel-storage.com"
        }
    ]
  },

  */
};

export default nextConfig;
